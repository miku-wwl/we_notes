spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        # prefix: 'config'
        # file-extension: yaml
        # namespace: xxxxxx
        # group: yyyyyy
  profiles:
    active: dev
  application:
    name: config